# TODO: Write notify(text) that sends Telegram messages using TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID from env.
# Also add notify_exception(label, e) that formats a traceback and calls notify.

import requests
